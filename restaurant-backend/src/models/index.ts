import { MenuItem } from './MenuItem';
import { Order } from './Order';

// Exportar todos los modelos
export { MenuItem, Order };

// Definir asociaciones si las hay
// MenuItem.hasMany(Order);
// Order.belongsTo(MenuItem);